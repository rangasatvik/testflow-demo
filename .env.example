PORT=8080
DATABASE_URL=postgres://testflow:testflow@localhost:5432/testflow

# AWS (for deploy workflow / infra scripts, not needed for local dev)
AWS_REGION=us-east-1
AWS_ECR_REPOSITORY=testflow-demo
